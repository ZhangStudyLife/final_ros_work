# Maps

Save the course map here after SLAM:

```bash
rosrun map_server map_saver -f ~/final_work/tb3_course_ws/src/tb3_sim_course/maps/turtlebot3_map
```

`navigation.launch` and `patrol.launch` use `turtlebot3_map.yaml` by default.
